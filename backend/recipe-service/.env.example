# Same Postgres instance as the other services. This service is read-only —
# recipes/recipe_ingredients/recipe_base_comparisons/recipe_ingredient_products/
# raw_ingredient_nutrients are all populated by the data team's recipe pipeline.
POSTGRES_HOST=211.46.52.151
POSTGRES_PORT=15432
POSTGRES_DB=test_db
POSTGRES_USER=여기에 입력
POSTGRES_PASSWORD=여기에 입력

FRONTEND_URL=http://localhost:3000
