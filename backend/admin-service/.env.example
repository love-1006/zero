# Must be identical to login-service's JWT_SECRET — admin-service only
# verifies admin-role tokens issued by login-service, it never issues its own.
JWT_SECRET=dev-secret-change-me
# Must match login-service's JWT_EXPIRE_MINUTES (sliding-session refresh).
JWT_EXPIRE_MINUTES=180

FRONTEND_URL=http://localhost:3000
